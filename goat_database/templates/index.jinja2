{% extends "goat_database:templates/layout.jinja2" %}

{% block content %}
    {% for item in goats %}
        <li><a href="{{ request.route_url('goat', id=item.id) }}">{{ item.name }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}
